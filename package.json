{
  "name": "trimble-file-explorer-extension",
  "version": "1.0.0",
  "description": "Extension pour explorer les fichiers d'un projet Trimble Connect",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "staticWebApp": {
    "routes": [
      {
        "route": "/manifest.json",
        "headers": {
          "Access-Control-Allow-Origin": "*",
          "Access-Control-Allow-Methods": "GET, OPTIONS",
          "Access-Control-Allow-Headers": "Content-Type"
        }
      }
    ]
  },
  "author": "Votre Nom",
  "license": "ISC"
}
